# Vite exposes variables prefixed with VITE_ to the frontend
# The Vite proxy in vite.config.js handles routing /api → localhost:5000
# so this is just for reference / direct API calls if needed
VITE_API_URL=http://localhost:5000/api
